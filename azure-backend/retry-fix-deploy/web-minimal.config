<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <system.webServer>
    <!-- indicates that the server-test-minimal.js file is a node.js application to be handled by the iisnode module -->
    <handlers>
      <add name="iisnode" path="server-test-minimal.js" verb="*" modules="iisnode" />
    </handlers>
    
    <!-- URL rewrite rules to redirect all requests to server-test-minimal.js -->
    <rewrite>
      <rules>
        <rule name="NodeInspector" patternSyntax="ECMAScript" stopProcessing="true">
          <match url="^server-test-minimal.js\/debug[\/]?" />
        </rule>
        <rule name="StaticContent">
          <action type="Rewrite" url="public{REQUEST_URI}" />
        </rule>
        <rule name="DynamicContent">
          <conditions>
            <add input="{REQUEST_FILENAME}" matchType="IsFile" negate="True" />
          </conditions>
          <action type="Rewrite" url="server-test-minimal.js" />
        </rule>
      </rules>
    </rewrite>
    
    <!-- CORS configuration for IIS -->
    <httpProtocol>
      <customHeaders>
        <add name="Access-Control-Allow-Origin" value="*" />
        <add name="Access-Control-Allow-Methods" value="GET, POST, PUT, DELETE, OPTIONS" />
        <add name="Access-Control-Allow-Headers" value="Content-Type, Authorization, X-Requested-With" />
      </customHeaders>
    </httpProtocol>
    
    <!-- iisnode configuration -->
    <iisnode 
      nodeProcessCommandLine="node"
      loggingEnabled="true"
      logDirectory="iisnode"
      debuggingEnabled="false"
      node_env="production"
    />
    
    <!-- Ensure Node.js errors are properly displayed -->
    <httpErrors existingResponse="PassThrough" />
  </system.webServer>
</configuration>